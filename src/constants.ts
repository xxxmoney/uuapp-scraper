
// Some proper user agent
export const USER_AGENT = 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/91.0.4472.124 Safari/537.36';

// Waits until there are no more than 0 network connections for at least 500 ms
export const WAIT_UNTIL_NETWORK_IDLE = 'networkidle0';

// Main url - used for authentication
export const MAIN_URL = 'https://www.plus4u.net';``

// Container selector for getting content of each sub page
export const CONTAINER_SELECTOR = '.uu5-bricks-page-content-body';

// Selector for links for all sub pages
export const LINK_SELECTOR = '.uu-bookkit-go-to-page-link';

// Selector for login button
export const LOGIN_BUTTON_SELECTOR = '.plus4u5-app-button';

// Selector for login form
export const AUTH_FORM_CONTAINER_SELECTOR = '.uu-identitymanagement-bricks-form-container-body';

// Selector for username input in the form
export const AUTH_FORM_USERNAME_SELECTOR = '.uu5-forms-text-input input[name="username"]';

// Selector for password input in the form
export const AUTH_FORM_PASSWORD_SELECTOR = '.uu5-forms-text-input input[name="password"]';

// Selector for submit button in the form
export const AUTH_FORM_SUBMIT_BUTTON_SELECTOR = '.button[type="submit"]';

// Where to write all the contents
export const OUTPUT_FILE = 'output.txt';
